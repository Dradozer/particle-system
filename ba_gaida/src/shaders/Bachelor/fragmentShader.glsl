#version 450

out vec4 FragColor;

void main()
{

	//float dist = length(cameraPos - pos.xyz) / 12.5;
	//float r = 255 * dist / 255;
	//float g = 165 * dist / 255;
	
	FragColor = vec4(1.0f, 0.0f, 0.0, 1.0f);
}